import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['mekari.com'], // Add the domain where your external images are hosted
  },
};

export default nextConfig;
